{
  "name": "nuxt-app",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "predeploy": "npm run generate",
    "deploy": "gh-pages -d dist -t true",
    "prelocal": "npm run generate",
    "local": "npm run preview"
  },
  "dependencies": {
    "@nuxt/content": "^2.13.4",
    "@pinia/nuxt": "^0.9.0",
    "@vueuse/core": "^12.1.0",
    "bootstrap": "^5.3.3",
    "bootstrap-icons": "^1.11.3",
    "nuxt": "^3.14.1592",
    "pinia": "^2.3.0",
    "prismjs": "^1.29.0",
    "vue": "latest",
    "vue-router": "latest"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "gh-pages": "^6.2.0",
    "sass": "^1.83.0"
  }
}
