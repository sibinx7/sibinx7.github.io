export type TechnologyStack = {
	id: number;       // Represents the unique identifier
	name: string;     // Name of the technology
	type: string;     // Type of the technology (e.g., "database")
	icon: string;     // CSS class or identifier for the icon
	link: string;     // URL or link (can be empty)
	color: string;    // Hex color code for the technology
};